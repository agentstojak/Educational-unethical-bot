# requirements.txt

# Discord.py for interacting with the Discord API
discord.py==2.3.2

# Requests for making HTTP requests
requests==2.31.0

# PyAutoGUI for GUI automation
PyAutoGUI==0.9.53

# Pillow for image processing
Pillow==10.0.0

# psutil for system and process utilities
psutil==5.9.5

# PyGetWindow for getting window information
PyGetWindow==0.0.9

# PyCryptodome for cryptographic recipes and primitives
pycryptodome==3.18.0

# SpeechRecognition for speech recognition
SpeechRecognition==3.10.0

# pyaudio for audio processing
pyaudio==0.2.11

# opencv-python for computer vision tasks
opencv-python==4.8.0.74

# numpy for numerical operations
numpy==1.26.0

# pandas for data manipulation and analysis
pandas==2.1.1

# flask for web server
flask==3.0.0

# geopy for geocoding
geopy==2.4.0

# pyperclip for clipboard operations
pyperclip==1.8.2

# python-dotenv for environment variable management
python-dotenv==1.0.0

# schedule for task scheduling
schedule==1.2.0

# win32crypt for decrypting Windows credentials
win32crypt==0.5.0

# sqlite3 for database operations (included in Python standard library)
# No need to install separately

# json for JSON operations (included in Python standard library)
# No need to install separately

# base64 for encoding/decoding operations (included in Python standard library)
# No need to install separately

# re for regular expressions (included in Python standard library)
# No need to install separately

# tempfile for temporary file operations (included in Python standard library)
# No need to install separately

# shutil for high-level file operations (included in Python standard library)
# No need to install separately

# sys for system-specific parameters and functions (included in Python standard library)
# No need to install separately

# os for interacting with the operating system (included in Python standard library)
# No need to install separately

# getpass for password prompting (included in Python standard library)
# No need to install separately

# ctypes for calling functions in DLLs/so files (included in Python standard library)
# No need to install separately

# subprocess for spawning new processes (included in Python standard library)
# No need to install separately

# platform for accessing platform-dependent functionality (included in Python standard library)
# No need to install separately

# datetime for manipulating dates and times (included in Python standard library)
# No need to install separately

# asyncio for asynchronous I/O (included in Python standard library)
# No need to install separately

# pynput for controlling and monitoring input devices (keyboard and mouse)
pynput==1.7.6

# zipfile for reading and writing ZIP files (included in Python standard library)
# No need to install separately

# webbrowser for opening web-based documents (included in Python standard library)
# No need to install separately

# glob for filename pattern matching (included in Python standard library)
# No need to install separately

# wave for reading and writing WAV files (included in Python standard library)
# No need to install separately

# winreg for accessing the Windows registry (included in Python standard library)
# No need to install separately
